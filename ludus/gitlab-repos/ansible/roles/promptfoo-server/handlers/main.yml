---
# Handlers for promptfoo-server role

- name: restart ollama
  ansible.builtin.systemd:
    name: ollama
    state: restarted
    daemon_reload: true

- name: restart chatbot
  ansible.builtin.systemd:
    name: chatbot
    state: restarted
    daemon_reload: true

---
# =============================================================================
# JichoDNS Production Inventory
# Single-server deployment: all services on one host
# =============================================================================

all:
  hosts:
    jichodns-prod:
      ansible_host: 23.150.68.201
      ansible_user: "{{ vault_ssh_user }}"
      ansible_password: "{{ vault_ssh_password }}"
      ansible_become_password: "{{ vault_ssh_password }}"
      ansible_ssh_common_args: "-o StrictHostKeyChecking=no"

  children:
    jichodns:
      hosts:
        jichodns-prod:
